# 로드맵

진행 상태는 **이 파일에만** 적는다. 각 조각은 게임 안에서 확인되는 얇은 단위다.
개념은 [map.md](map.md), 이야기는 [stories.md](stories.md).

## 첫 이야기: 밀 배달 (완성, 2026-09-23)

- [x] **1. 첫 인사**: 새 뼈대(그래프 형식 1, 기록 DB, 노드 4개, 에디터 그래프 목록). CI 그린(테스트 25), 게임 확인 완료(2026-09-23): 첫 접속 인사, 재접속 시 조용함, publish 거부.
- [x] **2. NPC 소환 + 우클릭 채팅**: 에디터 NPC 등록(`npcs.json`) → `/lorebench npc spawn` 배치(기록 DB) → `On NPC Interact`. 서버 이름 설정. CI 그린(테스트 36), 게임 확인 완료(2026-09-23): 소환·이름표·우클릭·무적/고정·여러 배치·재접속 유지·참조 중인 NPC 삭제 거부·정의 삭제 시 사라짐.
  - 알려진 함정: `remove`(가까운 하나)와 `remove <id>`(전부)가 인자 하나 차이라, Tab 자동완성으로 전부 삭제가 실행될 수 있다. 배치 하나를 고르는 방법은 보류(아래 후보).
- [x] **3. GeckoLib 외형**: NPC 정의 `model`·`idle`, 노드 `Play NPC Animation`, 모델 없으면 스티브. 테스트 모델 `chief`(Blockbench MCP, `animation.chief.wave`/`happy`). CI 그린(테스트 39), 게임 확인 완료(2026-09-23): 모델·idle 반복, 우클릭 시 happy 후 idle 복귀, 리소스팩 끄면 스티브.
- [x] **4. 퀘스트 공개 + 퀘스트 화면**: 에디터 퀘스트 작성(`quests.json`), 노드 `Quest State`·`Reveal Quest`, `J` 퀘스트 화면. id를 `<종류>_<무작위>`로 바꿈. 결정: [0004 id·기록 키](decisions/0004-ids-and-record-keys.md), [0005 퀘스트](decisions/0005-quests.md). CI 그린(테스트 53), 게임 확인 완료(2026-09-23): 없는 아이템 publish 거부, 자동완성에 NPC 이름, 공개 전 빈 화면, 우클릭으로 공개, `3/10`·`10/10` 진행과 제출 가능 표시, 밀을 버리면 진행 중으로 복귀, 재접속 유지, publish한 제목 즉시 반영, 참조 중인 퀘스트 삭제 거부, **두 클라이언트(Dev1·Dev2)에서 공개·진행이 각자 따로**.
- [x] **5. 제출 + 보상**: 이야기 전체 완성. 노드 `Complete Quest`, 나갈 때 기록 저장. 결정: [0005 완료](decisions/0005-quests.md). CI 그린(테스트 53), 게임 확인 완료(2026-09-23): 밀 정확히 10개 회수·에메랄드 5개·happy 애니메이션·"고맙네!", 완료 화면(회색·완료·`× 10`), 다시 우클릭하면 "덕분에 살았네"뿐(중복 보상 없음), 가방이 꽉 차면 보상이 발밑에, 나갈 때 기록 저장 후 강제 종료해도 완료 유지, 잘못 연결한 `Complete Quest`는 아무것도 바꾸지 않고 경고.

## 두 번째 이야기: 대장장이의 칼 (완성, 2026-09-23)

- [x] **1. 대장장이의 칼**: 철 5개를 주면 이름·인챈트 붙은 칼을 받는다. 보상 아이템을 `/give` 문법으로([0006](decisions/0006-item-syntax.md)). CI 그린(테스트 55), 게임 확인 완료(2026-09-23): 보상 이름·날카로움 II가 화면 툴팁과 실제 칼에 붙음, 컴포넌트 오타·없는 모드·목표의 `[...]`는 파서 메시지와 함께 publish 거부, **Gobber 모드 컴포넌트(`gobber2:boolean_component_type`, 능력 켜기)도 보상에 붙음**.
- [x] **2. 경비대장과 늑대 사냥**: `칼 만들기` 완료자에게만 공개, 늑대를 잡으면 `늑대 1/3`. 처치형 목표([0005](decisions/0005-quests.md)). CI 그린(테스트 60), 게임 확인 완료(2026-09-23): 없는 몹·같은 몹 두 줄 publish 거부, 공개 전 처치는 안 셈, 칼·활 처치가 `1/3`·`2/3`으로 바로 반영, 길들인 늑대·남의 처치는 안 셈, `3/3`에서 멈추고 제출 가능 표시, 목표 순서를 바꿔도 진행 유지, 재접속·재시작 후 유지.
- [x] **3. 사냥 보상**: 대장장이에게 가면 보상. 이야기 완성. 새 코드 없이 에디터(보상·그래프 C 연결)만으로. 게임 확인 완료(2026-09-23): 3/3에서 대장장이에게 보상, 완료 표시(`× 3`), 다시 말 걸어도 보상 없음, 완료 후 처치는 안 셈.

## 작가 도구

- [x] **손에 든 아이템으로 목표·보상 정하기**: 에디터 ✋ 버튼이 고른 플레이어의 주 손 아이템을 `/give` 한 줄로 채운다. 제출형 목표는 `/clear` 조건(적힌 컴포넌트만 비교, 숨은 표식으로 위조 방지). [0006](decisions/0006-item-syntax.md). CI 그린(테스트 60), 게임 확인 완료(2026-09-24): 플레이어 선택 기억, ✋로 이름·인챈트·내구도·Gobber 컴포넌트까지 한 줄로, 빈손·접속 안 함 안내, 모루로 이름만 바꾼 가짜는 `0/1`·표식 있는 진짜는 닳거나 이름이 바뀌어도 `1/1`, 진짜만 회수, 태그 목표(`#minecraft:logs`), 잘못된 조건 publish 거부. 기존 `minecraft:wheat` 목표도 예전처럼 퀘스트 완료까지 동작(2026-09-25 확인).
- [x] **데이터 초기화**: Gradle `reset`(콘텐츠·기록을 백업 폴더로). 실행 확인 완료(2026-09-25).
- [x] **한 사람의 퀘스트 되돌리기** (사용자 아이디어, 2026-09-26): 퀘스트 편집 화면의 Players 섹션에 이 퀘스트 기록이
  있는 모든 플레이어(접속 안 한 사람 포함, 이름은 서버의 접속 기록)와 진행을 보여주고, [Reset]으로 그 사람을 **받기 전**으로
  (상태·진행 기록 삭제 → 다시 제안받고 지급품도 다시 받음). 준 보상·지급품은 되가져가지 않고, 다른 퀘스트(이것을 선행으로 둔
  퀘스트 포함)는 그대로. 기록 형식 변경 없음(쌈). `GET /api/quest-players`, `POST /api/quest-reset`(publish처럼 인증 없음).
  기록 저장소에 "이 키를 가진 주인 찾기"(`ownersWith`)·접속 안 한 주인 읽기·쓰기(`peek`·`setAny`) 추가.
  CI 그린(테스트 89), 게임 확인 완료(2026-09-26): 목록에 상태·진행, 접속 중 Reset 시 J 화면 즉시 갱신, 접속 안 한 사람도
  (offline)으로 보이고 Reset 뒤 재접속하면 없음, 다른 퀘스트 그대로, 로그에 기록, Reset 뒤 그래프 공개로 지급품을 다시 받음.

## 작업대: 에디터 나누기 (완성, 2026-09-26)

두 이야기를 만들며 **그래프가 커지는 문제**가 드러났다. NPC 하나가 퀘스트 여럿을 다루면
`Quest State` 안에 `Quest State`가 겹친다(대장장이·경비대장 그래프, 지금은 git 기록에만). 이건 기능이 아니라
**작가(에디터 사용자, 지금은 사용자 본인)의 UX 문제**라서, 플레이어 이야기 옆에 **작가 시나리오**를
두 번째 축으로 둔다.

- [x] **1. 헤더 탭** (완성, 2026-09-25): 탭 모양 → 퀘스트 탭 → NPC 탭. 형식 변경은 퀘스트 폴더뿐.
  - [x] **탭 모양만 먼저**: 헤더에 **Graphs / Quests / NPCs**. Graphs는 지금 화면 그대로, Quests·NPCs는
    UX가 정해지지 않아 **비활성**(마우스를 올리면 "Not designed yet"). CI 그린(테스트 60), 브라우저 확인
    완료(2026-09-25): 탭 표시·Graphs 밑줄, 비활성 탭 클릭 무반응·안내 문구, 기존 편집·Publish 그대로.
    화면 나누기는 아래 제안대로 하되 UX를 정한 뒤에.
  - [x] **퀘스트 탭**: 왼쪽 **트리 사이드바**(폴더·퀘스트, 접기, `+ Folder`·`+ Quest`는 고른 폴더 안에) +
    가운데 **내용**(퀘스트 편집, 폴더 이름·위치·삭제). 폴더는 퀘스트 문서에 저장, 에디터 전용
    ([0008](decisions/0008-quest-folders.md)). 폴더를 지우면 안의 것이 한 단계 위로. Graphs 탭에서 퀘스트 목록은 빠짐.
    마지막 탭은 브라우저가 기억. 코드: `editor/src/QuestTab.jsx`. CI 그린(테스트 62), 게임 확인 완료(2026-09-25):
    기존 퀘스트가 맨 위에 그대로(초기화 불필요), 새로고침해도 탭 유지, 고른 폴더 안에 생성, Folder 선택으로 이동,
    Inside 목록에 자기·하위 폴더 없음, publish 후 새로고침해도 트리 유지, 빈 폴더 이름 publish 거부, 폴더 삭제 시
    안의 것이 위로, Graphs 탭에 퀘스트 목록 없음·노드의 퀘스트 선택은 그대로, J 화면에 폴더 없음·진행 유지.
  - [x] **NPC 탭**: 최대한 간단하게. 왼쪽 NPC 목록(`+ NPC`, 배치 수) + 가운데 이름·id와
    **섹션 카드**(Look: 모델·idle / Placed in the world: 배치 목록·소환 명령). 새 기능은 섹션을 하나씩 더한다
    (Skyrim CK 탭·Easy NPC 탭·Citizens 특성 참고. 특성 붙이기식은 기능이 여럿 생기면). 다음 섹션 후보: 대화(인사말,
    대화창 작업 때), 쓰이는 곳(나중에), 폴더(NPC가 많아지면 퀘스트 트리 재사용). NPC 형식·Java 변경 없음.
    Graphs 탭에서 NPC 목록은 빠짐(노드의 NPC 선택은 그대로). 탭이 모두 켜져 비활성 탭 처리는 걷어냄.
    코드: `editor/src/NpcTab.jsx`. CI 그린(테스트 62), 게임 확인 완료(2026-09-25): 이름순 목록·배치 수, Look·배치
    섹션 표시, 새 NPC publish 후 소환 자동완성, 소환 뒤 새로고침하면 배치 수·위치 반영, 모델 비우면 스티브·되돌리면
    복귀, 그래프가 쓰는 NPC 삭제는 publish 거부, 배치된 NPC 삭제 경고 후 게임에서 사라짐, Graphs 탭에 NPC 목록 없음·
    노드의 NPC 선택은 그대로.
  - [x] **NPC·그래프 폴더**: 퀘스트와 같은 방식으로 `npcs.json`·`graphs.json`에도 폴더 목록(문서마다 따로,
    [0008](decisions/0008-quest-folders.md)). NPC 탭 왼쪽이 트리(`+ Folder`·`+ NPC`, 배치 수). Graphs 탭 사이드바는
    **위아래 2단**: 위 트리(`+ Folder`·`+ Graph`), 아래 노드 팔레트. 폴더를 고르면 오른쪽 칸에 폴더 설정, 그래프
    설정에 Folder 선택. 고른 항목이 든 폴더는 접혀 있어도 펼쳐짐. 트리·폴더 편집은 `editor/src/FolderTree.jsx`,
    서버는 `Folders.java`로 모음. CI 그린(테스트 68), 게임 확인 완료(2026-09-25): 2단 사이드바·기존 그래프 맨 위에
    그대로(초기화 불필요), 고른 폴더 안에 새 그래프가 생기고 열림, Folder 선택으로 이동, 접힌 폴더 자동 펼침, NPC
    폴더·배치 수 유지, publish 후 새로고침해도 세 트리 유지, 그래프 폴더 삭제 시 안의 것이 위로, 그래프 동작(우클릭·
    접속 인사)은 폴더와 무관하게 그대로.
  - Graphs 탭은 그래프 트리 + 노드 팔레트, 캔버스, 노드·그래프·폴더 설정. Publish는 헤더에서 세 문서를 한 번에.
    상태(graphs·npcs·quests·folders)는 Publish 때문에 `App.jsx`에 있다.
- **탭 원칙** (2026-09-25, **열려 있음**: 쓰다가 안 맞으면 고친다. 탭 구성은 에디터 일이라 바꾸는 비용 쌈.
  비싼 건 탭 뒤의 문서를 나누거나 합치는 것):
  - 새 탭은 셋이 **모두** 맞을 때: ① 자기 목록(id가 있고 자기 문서에 저장) ② 자기 편집 화면(캔버스·타임라인·넓은
    폼) ③ 작가가 거기서 오래 일함. 하나라도 빠지면 기존 탭의 **섹션**이나 고르기 목록·팝업으로.
  - 합칠 때: 두 탭이 같은 대상을 편집할 때(예: 몹과 NPC가 둘 다 "모델 + 배치 + 대사"면 "캐릭터" 탭 하나에 종류
    구분), 탭이 너무 비었을 때. 한 작업에 두 탭을 오가면 합치기보다 **고르기 목록·바로가기 링크**를 먼저.
  - 탭이 6~7개를 넘으면 가로 탭 대신 왼쪽 세로 메뉴(VS Code), 전체 검색. 참고: RPG Maker 데이터베이스 탭,
    Blender 작업 공간, Creation Kit Object Window.
  - 적용: **애니메이션**은 애니메이션 파일이 리소스팩에 있고 편집은 Blockbench라 ①·②가 약하다 → 먼저 NPC 탭
    Look 섹션을 넓히고, 몹·뼈대 공유가 생기면 탭(이름은 "에셋"일 수도). **시네마틱**은 **카메라 기능이 필요해질 때**
    추가한다.
- [x] **2. 퀘스트 작업대 설계** (정함, 2026-09-25): [0009](decisions/0009-quest-workbench.md). 퀘스트 중심(필요하면
  대화 중심을 더함). 퀘스트에 `giver`·`receiver`·`requires`(완료만)·`lines`(제안·진행 중·제출, 대사는 배열), NPC에
  인사말. NPC 클릭 = 대화창: 할 수 있는 일(제출 가능 → 받을 수 있음)로 바로 시작, 없으면 인사말 + 목록. 진행 중은
  받는 NPC에게서 늘 한 줄(대사 없으면 고를 수 없음). "제안"은 저장하지 않아 플레이어 기록 형식은 그대로.
  보류: 그래프 연결, 진행 중 접기, 머리 위 `!`/`?`, 대사 줄마다 애니메이션·시선·말하는 사람·선택지.
  작가 시나리오 4개로 점검함(대사 고치기·선행 원인·퀘스트 순서·쓰이는 곳).
- [x] **3. 퀘스트 작업대 만들기** (완성, 2026-09-26, [0009](decisions/0009-quest-workbench.md)): 두 이야기가 그래프 없이
  퀘스트 문서와 대화창만으로 끝까지 돈다.
  - [x] **1. 형식 + 에디터**: 퀘스트에 `giver`·`receiver`·`requires`·`lines`, NPC에 `greeting`
    (형식 번호는 둘 다 1 그대로, 옛 문서도 읽힘). publish 검사: 없는 NPC(`QuestDoc.npcErrors`)·없는 선행 퀘스트·
    자기 자신이나 돌고 도는 선행 거부, 빈 대사 줄 거부(에디터는 빈 줄·빈 목록을 걸러서 보냄). 대사 읽기·쓰기는
    `DialogueLines`(퀘스트·인사말 공용). 에디터: 퀘스트 편집을 카드 4장(Basics / Flow / Dialogue / Needs and rewards),
    NPC 탭에 Dialogue 카드(인사말). 공용 `editor/src/Section.jsx`(카드·대사 줄 목록). **게임 동작은 아직 그대로.**
    CI 그린(테스트 73), 확인 완료(2026-09-26, reset 뒤 사용자 서버의 에디터에서 Claude가 직접): 카드 4장, 주는·받는
    NPC·선행·대사·인사말 입력, publish 후 새로고침해도 유지·빈 줄만 빠짐, 선행 순환 거부, 퀘스트가 가리키는 NPC 삭제
    거부, 서버 로그 오류 없음. 게임 우클릭은 그래프가 없어(reset) 확인 대상 없음.
  - [x] **2. 대화창**: NPC 우클릭 → 서버가 그 플레이어의 상태로 할 이야기를 계산
    (`Dialogue.plan`: 제출 가능 → 받을 수 있음 → 진행 중, 테스트 동반) → `DialoguePayload`로 그 플레이어에게만.
    화면(`DialogueScreen`): 할 수 있는 일로 바로 시작(대사 페이지 → 카드 [수락]/[거절] 또는 [건네기]/[나중에]),
    없으면 인사말 → 목록. 진행 중은 받는 NPC 목록에 늘 한 줄, 대사 없으면 누를 수 없음. 수락·건네기는
    `DialogueChoicePayload` → 서버(`Dialogues`)가 NPC가 그대로 있고 8칸 안인지, 아직 제안·제출 가능한지 다시 보고
    `Quests.reveal`/`complete`. `On NPC Interact` 그래프도 그대로 함께 돈다. 할 이야기도 인사말도 없는 NPC는 창을
    열지 않는다. 필요한 것·보상 그리기는 `QuestCard`로 빼서 J 화면과 공유. CI 그린(테스트 79), 게임 확인 완료
    (2026-09-26): 제안 대사 페이지 → 카드(`× 10`)·[수락]/[거절], 거절 후 목록에 `!`, 수락 후 회색 `?`·J 화면 `0/10`,
    진행 중 대사 → 진행 카드 → [돌아가기], 목표를 갖추면 바로 제출 대사 → [건네기]로 회수·보상·창 닫힘, 할 이야기도
    인사말도 없으면 창 안 뜸, 대사 없는 퀘스트는 바로 카드·진행 중은 누를 수 없음, 선행 미완료면 인사말만(제안 없음),
    선행 완료 뒤 바로 제안, 받는 NPC가 다르면 수락 뒤 창 닫힘, **Dev2는 따로**(인사말만), 처치형 `3/3` → [나중에] →
    목록 → [건네기], J 화면 그대로.
  - [x] **3. 대사 줄 애니메이션**: 두 이야기를 새 방식으로 되살리며 빠진 "촌장이 기뻐한다"를
    (가) 그래프 연결 대신 **(나) 대사 줄 애니메이션**으로(유스케이스 다이어그램으로 비교). 줄 = 글자 또는
    `{text, animation}`([0009](decisions/0009-quest-workbench.md) "대사 줄 애니메이션"). **대화한 사람 화면에서만** 재생
    (`DialogueScreen` → `NpcEntity.playLocally`). 에디터: 대사 줄마다 애니메이션 칸(선택). 테스트 `DialogueLinesTest`.
    CI 그린(테스트 82), 게임 확인 완료(2026-09-26): 에디터에서 제출 대사·인사말에 happy 넣고 publish 후 유지, 인사말
    페이지에서 촌장이 기뻐하고 idle 복귀, **옆의 다른 플레이어 화면에서는 안 보임**(Dev1·Dev2 양쪽으로), 애니메이션 없는
    줄은 그대로, 없는 애니메이션 이름에도 게임 멈추지 않음, 칸을 비우면 글자 줄로.

## 세 번째 이야기: 농부의 밭 (완성, 2026-09-26)

직접 기르고 번식시키는 이야기([stories.md](stories.md)). 새 목표 종류 둘 + 지급품 + 수락 뒤 대사.

- [x] **0. 설계** (정함, 2026-09-26): [0010](decisions/0010-farming-goals.md). 수확형(`harvest`, 작물 블록 id, 포기 수,
  분명한 작물만)·번식형(`breed`, 새끼 수, 먹인 사람 모두, 알 낳는 동물·주민 보류), 진행 기록 키 `<종류>:<대상 id>`(처치 포함),
  지급품(`supplies`, 진행 중이 되는 순간 한 번), 수락 뒤 대사(`lines.accepted`).
- [x] **1. 수확형 목표**: `harvest`(작물 블록 id), `BreakEvent`로 다 자란 것만 포기 수로 셈(크리에이티브 포함), publish
  검사(`Crops`: 바닐라 작물 틀·네더 사마귀·코코아, 다 자라면 다른 블록이 되는 토치플라워는 제외), 화면 아이콘은 집을 때
  나오는 것, 에디터 작물 목록(`GET /api/crops`). 진행 기록 키 `<종류>:<대상>`(처치 포함), 메시지 버전 2.
  CI 그린(테스트 84), 게임 확인 완료(2026-09-26, `reset` 뒤): 작물 목록·호박 거부, 받기 전 수확 무시, `Wheat Crops 0/3`,
  덜 자란 것·물로 쓸기·Dev2의 수확은 안 셈, 뼈가루로 키운 것·크리에이티브는 셈, 재접속 유지, `3/3`에서 멈춤, 처치형
  `Chicken 1/1`(새 키), 건네기 때 밀을 가져가지 않음, 완료 뒤 수확 무시.
- [x] **2. 번식형 목표**: `breed`(몹 id), `BabyEntitySpawnEvent`를 가장 늦게 들어 부모 두 마리에게 먹인 사람 모두 +1(새끼
  종류로 셈), publish 검사(`Breeding`: 몹을 만들어 봐서 먹이 번식 동물만, 거북·개구리·스니퍼·주민 거부), 에디터 동물 목록
  (`GET /api/animals`). 화면에 세는 목표의 종류를 붙임("소 번식 0/2", "소 처치 0/1", "밀 작물 수확"). CI 그린(테스트 85),
  게임 확인 완료(2026-09-26): 동물 목록·거북·주민 거부, 받기 전 번식 무시, 번식·처치가 따로 셈, Dev1·Dev2가 한 마리씩
  먹이면 둘 다 +1, 남이 먹인 번식은 안 셈, `2/2`에서 멈춤, 재접속 유지, 건네기·완료 뒤 무시.
- [x] **3. 지급품 + 수락 뒤 대사**: `supplies`(진행 중이 되는 순간 `Quests.reveal`에서 한 번, 꽉 차면 발밑)와
  `lines.accepted`(서버가 수락을 확인한 답장 `DialoguePayload.said`에 실어 보내고 화면이 먼저 보여줌), 제안 카드 "받는 것",
  에디터 "After accepting"·"Given on accepting". 형식 번호 1 그대로, 메시지 버전 3. CI 그린(테스트 86), 게임 확인 완료
  (2026-09-26): 칸 유지·오타 거부, 제안 카드에 받는 것, 수락 즉시 씨앗 → 수락 뒤 대사(애니메이션), 다시 받지 않음, 꽉 차면
  발밑, 대사 중 Esc여도 받음, 대사 없는 퀘스트는 예전처럼, 그래프 `Reveal Quest`로도 한 번만.
- [x] **4. 농부 이야기 조립**: 코드 없이 에디터로 농부·`밭일 배우기`·`소 키우기`를 만들고 처음부터 끝까지(`reset` 뒤,
  에디터 입력은 사용자 서버의 에디터에서 Claude가 직접). 게임 확인 완료(2026-09-26): 장면 1~6 — 제안 카드의 받는 것, 수락
  즉시 씨앗과 수락 뒤 대사, 선행 전엔 소 키우기 제안 없음, 수확 진행·진행 중 대사, 제출 뒤 곧바로 소 키우기 제안, 번식
  `2/2`, 기뻐하며 보상, 끝나면 인사말만, J 화면 둘 다 완료.

## 대화 몸짓 (진행 중)

말을 걸면 하던 동작을 멈추고 대화 자세로, 닫으면 돌아가는 NPC(사용자 제안, 대화 연출 개선에서). 장면은 0011.

- [x] **0. 설계** (정함, 2026-09-27): [0011](decisions/0011-talk-gestures.md). NPC 문서의 대화 한 벌
  `talk: { start, loop, end }`, 대사 줄 `animation`은 이름 또는 `{ name, play: "loop" }`, 말 건 사람 화면에서만,
  넘어가는 규칙, 돌아보기.
- [ ] **에셋**: chief 모델에 `talk_start`·`talk`·`talk_end`, 반복용 `point`(Blockbench, 저장소 밖).
- [ ] **1. 대화 한 벌**: `talk` 칸(형식 테스트), 대화창 열기·닫기에 따른 들어가기·반복·나가기, 줄 애니메이션 뒤 `talk.loop`로,
  에디터 Talk 칸.
- [ ] **2. 반복하는 대사 줄**: `animation` 묶음(`play: loop`, 형식 테스트), 페이지가 떠 있는 동안 반복, 에디터 Repeat.
- [ ] **3. 돌아보기**: 대화 중 말 건 사람 쪽으로, 닫으면 원래 방향.

## 다음 이야기 후보

- 도감(Completionist's Index 같은 것): 퀘스트와 같은 "공개" 방식으로 확장.
- 파티: 파티원 모두에게(대상), 파티가 함께 진행하는 퀘스트(기록의 주인 `party`).
- 대상 넓히기: 모든 플레이어, 상대방(사건이 역할 이름을 주고 노드가 고름).
- 기다리기(지연): 대사 사이 간격 등. legacy-v2의 틱 스케줄러 참고.
- 리소스팩 자동화: Lorebench가 자기 폴더(예: `config/lorebench/assets`)를 항상 켜진 리소스팩으로 등록하면
  모드팩에 Paxi 같은 모드가 필요 없다. 더 나아가 서버가 팩을 호스팅·푸시(이때 에디터 포트 8080을
  외부에 열면 인증 없는 publish API도 열리니 포트 분리나 인증 필요).
- NPC 배치 하나 고르기(보류): 후보는 명령어 분리(`remove` / `clear <id>`), 에디터 배치 목록의 삭제 버튼,
  게임 속 관리 GUI(조각 4에서 화면을 만든 뒤). 배치별 고유 키(`placement_<엔티티 UUID>`)는 이미 있다.
- 멀티서버(Velocity): 서버마다 Lorebench, 기록 DB 공유(주인에 서버 이름이 이미 있음). 남은 숙제:
  서버를 옮길 때 기록 순서(나갈 때 저장은 조각 5에서 함. 다만 A가 쓰기 전에 B가 읽는 경합은 남음), 콘텐츠(그래프·NPC·퀘스트 문서)를
  서버마다 둘지 공유 저장소에서 받을지.
- 애니메이션 공유: 지금 애니메이션은 모델 이름에 묶임(`animations/npc/<모델>`). **뼈대 규격**(예: `humanoid`)에
  묶으면 같은 뼈대의 모델끼리 공유(Bedrock 분리 파일·Unity Humanoid 방식). 몹 적용은 (가) Lorebench 전용 몹(쌈) /
  (나) 바닐라 몹 교체(GeckoLib `GeoReplacedEntityRenderer`, 한 마리만 바꿀 수 있는지 미확인) / (다) 바닐라 모델 직접
  움직이기(playerAnimator식, 비쌈). 뼈대 이름·파일 경로 규칙은 리소스팩에 남아 비쌈.
- NPC 일과표·상태 (사용자 방향: 규모 있는 멀티 RPG 서버, 2026-09-26): **상태마다 들어가기·반복·나가기 한 벌**을 두고,
  상태가 바뀌면 "지금 상태의 end → 다음 상태의 start"로 잇는다(위처 3 작업 장소 방식). 짝마다 화살표를 그리는
  상태 기계는 피한다. 단계: (1) 대화 한 벌 `talk: { start, loop, end }` → (2) 평소 동작도 한 벌 + 시간대별 일과표
  (`idle`은 글자도 계속 읽어 옛 문서 유지, 대화의 `talk.start`에 넣던 "하던 일 멈추기"는 평소 상태의 `end`로 옮김)
  → (3) 어색한 짝에만 **짝 전환 예외**(기본 잇기 위에 덮어쓰기, Unreal 상태 기계의 짝별 전환과 같은 생각).
  각 단계는 그걸 요구하는 이야기가 생길 때 설계한다. (2)의 숙제: 대화 중에 시간대가 바뀌면, 자리 이동, 누가 같은 자세를 보나.
- 형식 검사 메시지에 제목 함께: `quest 'quest_nj46mr8g' ends up requiring itself`처럼 id만 나오는 메시지를
  `'늑대 사냥' (quest_…)`처럼(publish 때 NPC 검사 메시지는 이미 그렇다). 내부 코드라 쌈.
- 대화 연출 개선 (사용자 제안, 2026-09-26):
  - **타이핑**: 대사를 한 글자씩(언더테일처럼 구간마다 빠르게·느리게). 화면은 쌈, 속도를 대사에 적는 방법(줄마다 칸 / 글
    안의 표시)은 문서에 남아 비쌈. 클릭하면 한 줄을 다 보여주기, 글자 소리는 함께 정할 거리.
    글 안 표시는 **속도와 동작 전환을 함께** 다룬다(사용자 예: "이 밭 좀 보게..."에서 `point` 반복 → "헉 해충이 꼬였잖아!"에서
    `shocked`, Ren'Py `{w}`·`{cps}`, Text Animator의 `<?이벤트>` 같은 방식). 한 번/반복은 줄의 `animation.play`와 같은 말을
    쓴다. 줄의 `animation`은 "맨 앞 표시"의 짧은 형태로 남는다.
  - **대화 몸짓**: 진행 중, 위 "대화 몸짓" 섹션.
  - **빛과 그림자**: 바닐라 렌더러에는 광원에 따른 실시간 그림자가 없고 셰이더팩(Iris 등)이 맡는 영역이라 모드 안에서
    만들기는 매우 어렵다. 셰이더팩과 GeckoLib NPC가 함께 잘 보이는지는 확인하지 않았다. 싼 대안: 발밑 둥근 그림자 크기,
    텍스처에 그려 넣은 명암.
- 번식 동물 목록 다듬기: `Breeding`은 "동물이면서 알을 낳지 않음"으로 고르므로, 번식으로는 태어나지 않는 앵무새·북극곰·
  스켈레톤 말·좀비 말도 에디터 목록과 publish 검사를 통과한다(그 목표는 영원히 못 끝냄). 노새는 말+당나귀로 태어나니 맞다.
  내부 코드라 쌈. 조각 4에서 에디터 입력 중 발견(2026-09-26).
- 거절 뒤·완료 뒤 대사: 수락 뒤 대사(`accepted`)는 농부 이야기 조각 3에서 만든다. 남은 빈자리는 거절 뒤 한마디,
  완료한 퀘스트를 준 NPC에게 다시 말 걸 때(옛 그래프 B의 "덕분에 살았네", 지금은 인사말만). 퀘스트 문서 칸이라 비쌈.
- 퀘스트 전용 아이템(메이플식, 사용자 아이디어): 퀘스트 진행 중에만 몹·채집에서 나오는 물건(예: "늑대가 물고 간 목걸이
  조각"). 제출형 목표로 모은다. 물건 자체가 이야기인 퀘스트에 맞다. 농부 이야기는 "직접 해보기"라 남에게 받을 수 없는
  수확형 목표로 갔다. 숙제: 다른 플레이어가 줍거나 넘겨받지 못하게(버리기 막기, 땅에 떨구지 않고 가방에 바로 넣기 등),
  죽으면 잃는지. 진짜 구별은 0006의 숨은 표식으로 가능. 드롭 규칙이 퀘스트 칸이라 비쌈.
- 알림 노드: 화면 오른쪽 위 알림(예: "퀘스트 완료: 밀 배달"). 지금 완료 알림은 그래프의 채팅·애니메이션에 맡긴다.
- `Complete Quest`의 `not_ready` 갈래: 기다리기 노드처럼 확인과 완료 사이에 틈이 생길 때 추가. `next`는 그대로라 옛 그래프는 안 깨진다.
- 결정 기록 규칙 맞추기: [README](README.md)는 "한 번 쓰면 고치지 않고, 바꿀 땐 새 기록으로 대체"인데,
  실제로는 0001·0002·0005·0006에 섹션을 덧붙이거나 일부를 고쳤다(예: 0005 "완료"·"처치형 목표", 0006
  "`/clear` 문법"). 규칙을 실제 방식(같은 주제의 확장은 덧붙이고, 뒤집는 결정은 새 기록)에 맞출지, 앞으로
  새 기록으로 나눌지 정한다.
- 옛 아이디어 목록: `legacy-v2`의 `docs/ideas.md`.
